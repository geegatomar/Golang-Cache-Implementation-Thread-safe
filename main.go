package main

func main() {
	// Check the main code for stress testing with multiple goroutines in main_stress_test.go file which shows
	// the full usage of the cache with multiple goroutines trying to simultaneously access the cache,
	// and the ability to handle that since its thread-safe
}
